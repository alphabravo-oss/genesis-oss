version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  # This API deliberately returns shared domain messages (ScanJob, ScanSettings).
  except:
    - RPC_REQUEST_RESPONSE_UNIQUE
    - RPC_RESPONSE_STANDARD_NAME
breaking:
  use:
    - FILE
