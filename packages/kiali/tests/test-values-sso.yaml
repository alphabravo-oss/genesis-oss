kiali:
  enabled: true
  sso:
    enabled: true
  values:
    upstream:
      cr:
        spec:
          auth:
            strategy: openid
certManager:
  enabled: true
monitoring:
  enabled: true
  values:
    bbtests:
      enabled: false
grafana:
  enabled: true
  values:
    bbtests:
      enabled: false
addons:
  keycloak:
    enabled: true
    values:
      bbtests:
        enabled: true
  minioOperator:
    enabled: true
    values:
      bbtests:
        enabled: false