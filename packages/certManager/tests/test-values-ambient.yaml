certManager:
  enabled: true
  values:
    issuers:
      selfSigned:
        enabled: true
        createOnInstall: false
    bbtests:
      enabled: true
      scripts:
        enabled: true

# Enable Ambient
istio:
  ambient:
    enabled: true
