apiVersion: v1
kind: Pod
metadata:
  name: registry-pod
spec:
  restartPolicy: Never
  terminationGracePeriodSeconds: 0
  initContainers:
    - name: init
      image: replace.image.registry/ironbank/redhat/ubi/ubi9-micro:9.7
      command: ["true"]
  containers:
    - name: test
      image: replace.image.registry/ironbank/redhat/ubi/ubi9-micro:9.7
      command: ["true"]
