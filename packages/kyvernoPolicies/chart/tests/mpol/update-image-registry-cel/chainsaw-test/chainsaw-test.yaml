apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: update-image-registry-cel
spec:
  timeouts:
    cleanup: 2m
  steps:
  - name: step-01
    try:
    - apply:
        file: ../policy.yaml
    - patch:
        resource:
          apiVersion: policies.kyverno.io/v1
          kind: MutatingPolicy
          metadata:
            name: update-image-registry-cel
          spec:
            matchConditions:
              - name: scope-to-test-ns
                expression: (cel; "object.metadata.namespace == '" + string(bindings.resolve("namespace")) + "'")
    - assert:
        file: policy-ready.yaml
  - name: step-02
    try:
    - apply:
        file: pod-registry.yaml
    - assert:
        file: pod-registry-mutated.yaml
    - apply:
        file: pod-untouched.yaml
    - assert:
        file: pod-untouched-unchanged.yaml
