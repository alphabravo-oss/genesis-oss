apiVersion: cli.kyverno.io/v1alpha1
kind: Test
metadata:
  name: add-default-capability-drop-cel
policies:
- ../policy.yaml
resources:
- resource.yaml
# For MPols, `kyverno test` proves the rule evaluates successfully against
# these fixture shapes offline. The explicit post-mutation object assertions
# live in the sibling Chainsaw test.
results:
- policy: add-default-capability-drop-cel
  rule: autogen-mutate
  resources:
  - bare-pod
  - partial-pod
  - full-pod
  - init-pod
  - ephemeral-pod
  kind: Pod
  result: pass
