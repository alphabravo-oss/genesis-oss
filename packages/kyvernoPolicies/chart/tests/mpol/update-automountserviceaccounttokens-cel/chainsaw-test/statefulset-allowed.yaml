apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: api-access-allowed-stateful
  namespace: update-automountserviceaccounttokens-2
spec:
  serviceName: api-access-allowed-stateful
  replicas: 1
  selector:
    matchLabels:
      app: api-access-allowed-stateful
  template:
    metadata:
      labels:
        app: api-access-allowed-stateful
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 0
      securityContext:
        runAsNonRoot: true
      containers:
        - name: test
          image: registry1.dso.mil/ironbank/redhat/ubi/ubi9-micro:9.7
          command: ["true"]
          securityContext:
            capabilities:
              drop:
                - ALL
