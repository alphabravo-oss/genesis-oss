apiVersion: v1
kind: Pod
metadata:
  name: badpod-hostpath-disallowed
spec:
  containers:
  - name: container01
    image: registry1.dso.mil/ironbank/redhat/ubi/ubi9-minimal
    volumeMounts:
    - name: host
      mountPath: /host-etc
  volumes:
  - name: host
    hostPath:
      path: /etc
