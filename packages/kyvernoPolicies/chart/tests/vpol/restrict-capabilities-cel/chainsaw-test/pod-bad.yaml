apiVersion: v1
kind: Pod
metadata:
  name: badpod-add-sys-admin
spec:
  containers:
  - name: container01
    image: registry1.dso.mil/ironbank/redhat/ubi/ubi9-minimal
    securityContext:
      capabilities:
        drop: ["ALL"]
        add: ["SYS_ADMIN"]
