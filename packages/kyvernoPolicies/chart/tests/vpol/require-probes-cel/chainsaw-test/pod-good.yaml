apiVersion: v1
kind: Pod
metadata:
  name: goodpod
spec:
  containers:
  - name: container01
    image: registry1.dso.mil/ironbank/redhat/ubi/ubi9-micro:9.7
    readinessProbe:
      exec:
        command: ["true"]
      periodSeconds: 10
    livenessProbe:
      exec:
        command: ["true"]
      periodSeconds: 10
