{{- $name := "require-labels-cel" }}
{{- if and .Values.enabled (dig $name "enabled" false .Values.celPoliciesBeta) }}
{{- $require := dig $name "parameters" "require" nil .Values.celPoliciesBeta -}}
{{- if not $require -}}
  {{- fail (printf "%s is enabled but celPoliciesBeta.%s.parameters.require is empty - disable the policy or provide a list of required labels" $name $name) -}}
{{- end -}}
apiVersion: policies.kyverno.io/v1
kind: ValidatingPolicy
metadata:
  name: {{ $name }}
  annotations:
    policies.kyverno.io/title: Require Labels
    policies.kyverno.io/category: Best Practices
    policies.kyverno.io/severity: {{ default "low" (dig $name "severity" nil .Values.celPoliciesBeta) }}
    policies.kyverno.io/subject: Pod
    policies.kyverno.io/description: >-
      A common set of labels on resources allows tools to work interoperably, describing objects
      in a common manner that all tools can understand and query.  This policy requires the use
      of labels and values contained in the specified list.
    policies.bigbang.dev/lifecycle: beta
    policies.bigbang.dev/origin: https://repo1.dso.mil/big-bang/product/packages/kyverno-policies
  labels: {{- include "kyverno-policies.labels" . | nindent 4 }}
spec:
  {{- include "bb-kyverno-policies.validationActions" (merge (dict "name" $name) .) | indent 2 }}
  failurePolicy: {{ include "bb-kyverno-policies.vpol-failurePolicy" (merge (dict "name" $name) .) }}
  {{- include "bb-kyverno-policies.cel-webhookTimeoutSeconds" (merge (dict "name" $name) .) | indent 2 }}
  {{- include "bb-kyverno-policies.cel-autogenControllers" (merge (dict "name" $name) .) | indent 2 }}
  evaluation:
    background:
      enabled: {{ include "bb-kyverno-policies.cel-background" (merge (dict "name" $name) .) }}
  {{- if dig $name "matchConstraints" nil .Values.celPoliciesBeta }}
  matchConstraints:
    {{- toYaml (dig $name "matchConstraints" nil .Values.celPoliciesBeta) | nindent 4 }}
  {{- else }}
  matchConstraints:
    resourceRules:
      - apiGroups:   [""]
        apiVersions: ["v1"]
        operations:  ["CREATE", "UPDATE"]
        resources:   ["pods"]
  {{- end }}
  {{- $nsExclude := include "bb-kyverno-policies.cel-excludeNamespaces" (merge (dict "name" $name) .) -}}
  {{- $userConditions := dig $name "matchConditions" nil .Values.celPoliciesBeta -}}
  {{- if or $nsExclude $userConditions }}
  matchConditions:
    {{- if $nsExclude }}
    {{- $nsExclude | nindent 4 }}
    {{- end }}
    {{- with $userConditions }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- end }}
  variables:
  - name: labels
    expression: "object.metadata.?labels.orValue({})"
  validations:
  {{- range $r := $require }}
    {{- $celKey := $r.key | replace "\\" "\\\\" }}
    {{- if $r.value }}
    {{- $celValue := $r.value | replace "\\" "\\\\" }}
    - expression: >-
        variables.labels.exists(k, k.matches('^{{ $celKey }}$') && variables.labels[k].matches('^{{ $celValue }}$'))
      message: >-
        A label with key matching pattern '{{ $r.key }}' with value matching pattern '{{ $r.value }}' is required.
    {{- else }}
    - expression: >-
        variables.labels.exists(k, k.matches('^{{ $celKey }}$') && variables.labels[k] != '')
      message: >-
        A label with key matching pattern '{{ $r.key }}' and a non-empty value is required.
    {{- end }}
  {{- end }}
{{- end -}}
