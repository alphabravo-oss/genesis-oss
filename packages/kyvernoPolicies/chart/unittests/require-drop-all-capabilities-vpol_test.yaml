# yaml-language-server: $schema=https://raw.githubusercontent.com/helm-unittest/helm-unittest/main/schema/helm-testsuite.json
suite: require-drop-all-capabilities-cel ValidatingPolicy
templates:
  - templates/pod-security-vpol/restricted/require-drop-all-capabilities-cel.yaml

capabilities:
  apiVersions:
    - policies.kyverno.io/v1
tests:
  - it: should render when enabled
    set:
      enabled: true
      celPoliciesBeta:
        require-drop-all-capabilities-cel:
          enabled: true
    asserts:
      - isKind:
          of: ValidatingPolicy
      - equal:
          path: metadata.name
          value: require-drop-all-capabilities-cel

  - it: should not render when disabled
    set:
      enabled: true
      celPoliciesBeta:
        require-drop-all-capabilities-cel:
          enabled: false
    asserts:
      - hasDocuments:
          count: 0

  - it: should not render when global enabled is false
    set:
      enabled: false
      celPoliciesBeta:
        require-drop-all-capabilities-cel:
          enabled: true
    asserts:
      - hasDocuments:
          count: 0
