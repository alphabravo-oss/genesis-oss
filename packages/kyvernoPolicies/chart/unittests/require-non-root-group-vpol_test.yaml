# yaml-language-server: $schema=https://raw.githubusercontent.com/helm-unittest/helm-unittest/main/schema/helm-testsuite.json
suite: require-non-root-group-cel ValidatingPolicy
templates:
  - templates/pod-security-vpol/restricted/require-non-root-group-cel.yaml

capabilities:
  apiVersions:
    - policies.kyverno.io/v1
tests:
  - it: should render when enabled
    set:
      enabled: true
      celPoliciesBeta:
        require-non-root-group-cel:
          enabled: true
    asserts:
      - isKind:
          of: ValidatingPolicy
      - equal:
          path: metadata.name
          value: require-non-root-group-cel

  - it: should not render when disabled
    set:
      enabled: true
      celPoliciesBeta:
        require-non-root-group-cel:
          enabled: false
    asserts:
      - hasDocuments:
          count: 0

  - it: should not render when global enabled is false
    set:
      enabled: false
      celPoliciesBeta:
        require-non-root-group-cel:
          enabled: true
    asserts:
      - hasDocuments:
          count: 0

  - it: has three validations for runAsGroup, fsGroup, and supplementalGroups
    set:
      enabled: true
      celPoliciesBeta:
        require-non-root-group-cel:
          enabled: true
    asserts:
      - lengthEqual:
          path: spec.validations
          count: 3
