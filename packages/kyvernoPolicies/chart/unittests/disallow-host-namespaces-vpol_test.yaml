# yaml-language-server: $schema=https://raw.githubusercontent.com/helm-unittest/helm-unittest/main/schema/helm-testsuite.json
suite: disallow-host-namespaces-cel ValidatingPolicy
templates:
  - templates/pod-security-vpol/baseline/disallow-host-namespaces-cel.yaml

capabilities:
  apiVersions:
    - policies.kyverno.io/v1
tests:
  - it: should render when enabled
    set:
      enabled: true
      celPoliciesBeta:
        disallow-host-namespaces-cel:
          enabled: true
    asserts:
      - isKind:
          of: ValidatingPolicy
      - equal:
          path: metadata.name
          value: disallow-host-namespaces-cel

  - it: should not render when disabled
    set:
      enabled: true
      celPoliciesBeta:
        disallow-host-namespaces-cel:
          enabled: false
    asserts:
      - hasDocuments:
          count: 0

  - it: should not render when global enabled is false
    set:
      enabled: false
      celPoliciesBeta:
        disallow-host-namespaces-cel:
          enabled: true
    asserts:
      - hasDocuments:
          count: 0
