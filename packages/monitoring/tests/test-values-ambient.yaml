#########################
# Required for package #
########################
monitoring:
  enabled: true
  values:
    bbtests:
      enabled: true
certManager:
  enabled: true
kyverno:
  values:
    bbtests:
      enabled: false
kyvernoPolicies:
  values:
    bbtests:
      enabled: false

# Enable Ambient
istio:
  ambient:
    enabled: true
